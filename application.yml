spring:
  profiles:
    active: dev  # default to dev locally unless overridden
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/marketplace}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

server:
  port: ${PORT:8080}

allowed:
  origins: ${ALLOWED_ORIGINS:http://localhost:3000}